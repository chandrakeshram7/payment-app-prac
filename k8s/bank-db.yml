apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: bank-db
spec:
  selector:
    matchLabels:
      app: bank-db
  serviceName: bank-db-service
  replicas: 1
  template:
    metadata:
      labels:
        app: bank-db
    spec:
      containers:
      - name: bank-db
        image: postgres:16
        resources:
          limits:
            memory: "200Mi"
            cpu: "200m"
          requests:
            memory: "100Mi"
            cpu: "100m"
        env:
          - name: POSTGRES_USER
            valueFrom:
              secretKeyRef:
                name: my-secret
                key: BANK_DB_USER
          - name: POSTGRES_DB
            valueFrom:
              secretKeyRef:
                name: my-secret
                key: BANK_DB_NAME
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: my-secret
                key: BANK_DB_PASSWORD
          - name: PGDATA
            valueFrom:
              configMapKeyRef:
                name: my-config
                key: BANK_PG_DATA
        ports:
        - containerPort: 5432
          name: postgres
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 1Gi
      storageClassName: "standard-rwo"
      volumeMode: Filesystem


---

apiVersion: v1
kind: Service
metadata:
  name: bank-db-service
spec:
  clusterIP: None
  selector:
    app: bank-db
  ports:
  - port: 5432
    targetPort: 5432
